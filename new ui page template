import React, { useState, useEffect } from 'react';
import PropTypes from 'prop-types';
import { makeStyles } from '@mui/styles'; // Temporarily using makeStyles
import Grid from '@mui/material/Grid';
import properties from '../../properties/properties';
import { Input } from '../../components/genericComponents/Input';
import { Box, Checkbox } from '@mui/material';
import { Link, useNavigate, useLocation } from 'react-router-dom';
import { getCommonFunctions } from '../serviceRevamp/add/ci_flow/SourceDetails';
import GenerateURL from '../../util/APIUrlProvider';
import { ValidateDataSet, VALIDATION_TYPE_REQUIRED } from '../../util/Validator';
import InvokeApi, { PostData } from '../../util/apiInvoker';
import { ErrorComponent } from '../utils/Error';
import { Loading } from '../utils/Loading';
import AlertStrip from '../../components/AlertStrips';
import { useCustomSnackbar } from '../../contexts/SnackbarContext';
import { safeNavigateBack, showErrorHandlerUpdated, RemoveFromArray } from '../../util/util';
import Button from '../../components/genericComponents/Button';
import StaticPartConfiguration from '../integrations/releasePackageConfiguration/configurationSetup/StaticPart';
import TopBar from '../../layouts/WhiteTopMinimal/component/TopBar';
import GenericSkeleton from '../../components/genericComponents/Skeletons/GenericSkeleton';

export default function AddPolicyTemplatee(props) {

    const classes = useStyles();

    return (
        <>
            <div style={{ display: 'flex', flexDirection: 'column', height: '100%', width: '100%', overflow: 'hidden' }}>
                <TopBar />
                <div className='d-grid' style={{ gridTemplateColumns: 'var(--space-320) 1fr', height: 'calc(100vh - var(--space-50))', marginTop: 'var(--space-50)', width: '100%' }}>
                    <StaticPartConfiguration
                        showAboutChip={true}
                        mainHeading={'What are External Groups'}
                        descriptionText={`External Groups help organizations manage and assign user groups from external identity providers. By mapping external groups to BuildPiper permission groups, administrators can automate user access management, simplify onboarding, and ensure consistent access control across applications, environments, pipelines, and deployments.`}
                    />
                    <div style={{ display: 'flex', flexDirection: 'column', height: '100%', width: '100%', overflow: 'hidden' }}>
                        <div className={classes.wrapbox} style={{ flex: 1, overflowY: 'auto', paddingTop: 'var(--space-15)' }}>
                            <Grid container>
                                <Grid item xs={10}>
                                    <Button
                                        variant="back"
                                        startIcon="ri-arrow-left-s-line"
                                        onClick={() => safeNavigateBack(navigate, '/role-groups')}
                                        style={{
                                            paddingLeft: 'var(--space-0)',
                                        }}
                                    >
                                        Back
                                    </Button>
                                   
                                </Grid>
                            </Grid>
                        </div>

                        <div className='' style={{ display: 'flex', justifyContent: 'flex-end', alignItems: 'center', background: 'var(--color-white)', padding: 'var(--space-16) var(--space-34)', marginTop: 'auto' }}>
                            <Link to="/role-groups"><button className="btn btn-outline-grey mr-10">Close</button></Link>
                            <Button className='btn' endIcon={"ri-arrow-right-s-line"} style={{ fontWeight: 'var(--font-weight-600)' }}>{'Save'} </Button>
                        </div>
                    </div>
                </div>
            </div>
        </>
    );
}

AddPolicyTemplatee.propTypes = {
    ...PropTypes.objectOf(PropTypes.any),
};


const useStyles = makeStyles({
    wrapbox: {
        padding: 'var(--space-32) var(--space-32) 0 var(--space-32)',
        backgroundColor: 'var(--color-white)',
        width: '100%',
        '& .close': {
            textAlign: 'right',
        },
    },
});
